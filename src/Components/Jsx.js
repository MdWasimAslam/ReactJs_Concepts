import React from "react";

function jsx() {
  return (
    <h3>
      JSX allows you to write HTML-like syntax in your JavaScript, which then
      gets transpiled into JavaScript and allows you to manipulate the DOM in a
      more readable way.
    </h3>
  );
}

export default jsx;
